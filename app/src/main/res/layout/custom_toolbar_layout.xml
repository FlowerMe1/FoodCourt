<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data></data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:orientation="vertical">

        <android.support.v7.widget.SearchView
            android:id="@+id/stores_search_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/search_view_top_bottom_margin"
            android:layout_marginLeft="@dimen/search_view_left_right_margin"
            android:layout_marginRight="@dimen/search_view_left_right_margin"
            android:layout_marginTop="@dimen/search_view_top_bottom_margin"
            android:background="@drawable/search_view_bg"
            android:iconifiedByDefault="true"
            app:closeIcon="@null"
            app:queryHint="@string/filter_hint"
            app:searchIcon="@null" />

    </LinearLayout>
</layout>